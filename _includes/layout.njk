<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{{ title }}</title>
    <style>
      * {
        margin: 0px;
      }

      body {
        background-color: rgba(255, 240, 245, 0.454);
        line-height: 1.4rem;
      }

      h2,
      h3 {
        color: #874a8c;
      }

      h3 {
        text-decoration: underline;
        margin: 0.6rem 0rem 0.4rem 0rem;
      }

      code {
        background-color: rgba(255, 192, 203, 0.253);
        color: #94618e;
        padding: 0.2rem;
      }

      main {
        width: 50vw;
        background-color: #fce8e8;
        color: #2c2c2c;
        box-shadow: 2px 2px 8px grey;
        border-radius: 0.3rem;
        font-family: system-ui, sans-serif;
        padding: 1rem 1.2rem 1rem 1.2rem;
        margin: 2rem auto;
      }
    </style>
  </head>
  <body>
    <main>{{ content | safe }}</main>
  </body>
</html>
